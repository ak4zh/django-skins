============
Installation
============

At the command line::

    $ easy_install django-skins

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-skins
    $ pip install django-skins
